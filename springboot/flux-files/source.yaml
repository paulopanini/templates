---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: ${{ values.name }}
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    semver: "1.x"
  url: ${{ values.repoUrl }}